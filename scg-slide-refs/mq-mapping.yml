apiVersion: "tanzu.vmware.com/v1"
kind: SpringCloudGatewayMapping
metadata:
  name: mq-mapping
  namespace: default
spec:
  gatewayRef:
    name: api-gateway
    namespace: default
  routeConfigRef:
    name: mq-routes